document.getElementById('appointmentForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Aquí podríamos agregar lógica para enviar los datos del formulario a un servidor o realizar validaciones adicionales
    alert('¡Cita médica programada con éxito!');
});
